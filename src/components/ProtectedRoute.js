// import React from "react";
// import { Route, Navigate } from "react-router-dom";

// const ProtectedRoute = ({ path, element: Component }) => {
//   // Replace this condition with your own logic to check if the user is logged in
//   const isLoggedIn = localStorage.getItem("sessionToken") !== null;

//   return (
//     <Route
//       path={path}
//       element={isLoggedIn ? <Component /> : <Navigate to="/dashboard" />}
//     />
//   );
// };

// export default ProtectedRoute;
